@model AutoskolaWeb.Model.Question

@{
    ViewBag.Title = "Izmjeni pitanje";
}

<h2>Izmjeni pitanje</h2>

@using (Html.BeginForm())
{

    <text>
        @Html.Partial("_CreateOrEdit")
        @Html.HiddenFor(p => p.ID)
    </text>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@using (Html.BeginForm())
{
    <text>
        @Html.Partial("_AnswerTable", Model.Answers.ToList())
        @Html.HiddenFor(p => p.ID, new { id = "QuestionId" })

    </text>
}