@model AutoskolaWeb.Model.Quiz

@{
    ViewBag.Title = "Edit quiz";
}

<h2>Edit quiz</h2>

@using (Html.BeginForm()) 
{
    
    <text>
        @Html.Partial("_CreateOrEdit")
        @Html.HiddenFor(p => p.ID)
    </text>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@using (Html.BeginForm())
{ 
    <text>
        @Html.Partial("_QuestionTable", Model.Questions.ToList())
        @Html.HiddenFor(p => p.ID, new { id = "QuizId" })
      
    </text>
}
